{# Page Name: Single Default Post #}

{% extends "layouts/base.twig" %}

{% block content %} 

  <section class="post-intro">
    <h1>{{ post.title }}</h1>
    <p>{{ post.get_content }}</p>
  </section>
  
{% endblock %}